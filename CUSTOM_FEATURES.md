# PassWall Custom Features

## 1. 自定义更新按钮
- 目标：在 LuCI 页面里增加一个“更新我的定制版 PassWall”按钮
- 作用：不是更新官方，而是更新我的 fork / release 版本
- 要求：
  - 能检查新版本
  - 能执行更新
  - 能显示成功/失败结果
  - 后续可接 GitHub release / raw ipk / 自建软件源

## 2. 节点整合成订阅链接
- 目标：把当前 PassWall 节点列表导出/整合成一个可复制的订阅文本
- 页面位置：节点列表 / 节点订阅页
- 逻辑：
  - 将现有节点转换为 share links 文本
  - 支持按分组导出
  - 支持一键复制
  - 后续可扩展为 base64 订阅输出

## 3. 失效节点处理
- 目标：处理已经失效的脚本/逻辑，增加批量检测与筛选失效节点能力
- 页面位置：节点列表
- 逻辑：
  - 批量检测延迟/连通性
  - 标记超时/失效节点
  - 为后续一键删除或生成有效订阅做准备

## 4. 检测延迟按钮增强
- 目标：在节点列表增加更直接的批量延迟检测入口
- 页面位置：节点列表
- 逻辑：
  - 保留单节点检测
  - 增加全组/全部节点批量检测
  - 结果直接回填到列表
  - 增加自动检测方式下拉：Ping / TCP Ping / URL Test

## 5. 自定义更新落地
- 目标：通过 GitHub Release 更新老高自己的 PassWall 定制版
- 页面位置：节点订阅页
- 逻辑：
  - 优先读取 release 中的 manifest.json
  - 按 manifest 的 packages 顺序下载并安装
  - 无 manifest 时回退为单文件 ipk 安装
  - 返回安装日志
  - 后续扩展版本比对

## 6. 节点导出体验增强
- 目标：同时支持 raw/base64 两种导出方式
- 页面位置：节点订阅页 / 节点列表页
- 逻辑：
  - raw：一行一个分享链接，便于人工检查
  - base64：直接作为订阅内容使用
